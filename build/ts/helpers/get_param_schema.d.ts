import { SwaggerRef } from "../types/swagger_ref";
import { SwaggerCustomParam } from "../types/swagger_custom_param";
export declare const getParamSchema: (value: any) => SwaggerRef | SwaggerCustomParam;
